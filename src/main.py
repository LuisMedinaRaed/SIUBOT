from interfaz_grafica import crear_interfaz

def main():
    """
    Función principal que inicia la interfaz gráfica de la aplicación.

    Ejecuta la función `crear_interfaz` desde el módulo `interfaz_grafica` para iniciar la interfaz gráfica de la
    aplicación principal.

    Nota:
    - Asegúrate de que todas las dependencias estén instaladas y de que el entorno esté configurado antes de ejecutar
      esta función.
    """
    crear_interfaz()
    
if __name__ == "__main__":
    main()